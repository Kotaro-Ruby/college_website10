<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <title><%= @college.college %> - 詳細</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f7fafc;
      color: #2d3748;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 20px;
    }
    header {
      text-align: center;
      margin-bottom: 40px;
    }
    header h1 {
      font-size: 2.8rem;
      font-weight: 700;
      color: #2b6cb0;
    }
    .university-info {
      font-size: 1rem;
      color: #4a5568;
      margin-top: 10px;
    }
    .university-info p strong {
      color: #38a169;
    }
    .details-section {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 40px;
    }
    .detail-card {
      background: #fff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      text-align: center;
    }
    .detail-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }
    .detail-card h2 {
      font-size: 1.4rem;
      margin-bottom: 10px;
      color: #2d3748;
    }
    .detail-card p {
      font-size: 1.1rem;
      color: #4a5568;
    }
    .major-box {
      background: #fff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      margin-top: 40px;
      margin-bottom: 40px;
    }
    .ranking-activities-box {
      display: flex;
      gap: 30px;
      margin-bottom: 40px;
      flex-wrap: wrap;
    }
    .ranking-box, .activities-box {
      flex: 1;
      background: #fff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      min-width: 300px;
      height: 350px;
    }
    .ranking-box h2, .activities-box h2, .major-box h2 {
      font-size: 1.4rem;
      margin-bottom: 15px;
      color: #2d3748;
      font-weight: 700;
    }
    .ranking-box ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .ranking-box li {
      font-size: 1.1rem;
      color: #4a5568;
      margin-bottom: 12px;
      line-height: 1.6;
      border-bottom: 1px solid #e2e8f0;
      padding-bottom: 8px;
    }
    .ranking-box li:last-child {
      border-bottom: none;
    }
    .media-section {
      display: flex;
      gap: 30px;
      flex-wrap: wrap;
      margin-bottom: 40px;
    }
    .media-item {
      flex: 1;
      min-width: 300px;
      background: #fff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .campus-photo img {
      width: 100%;
      border-radius: 10px;
      transition: transform 0.3s ease;
      max-height: 280px;
      object-fit: cover;
    }
    .campus-photo img:hover {
      transform: scale(1.05);
    }
    .social-links {
      display: flex;
      justify-content: space-evenly;
      gap: 15px;
      margin-top: 20px;
    }
    .social-links a {
      font-size: 1.5rem;
      text-decoration: none;
    }
    .back-button {
      display: block;
      margin: 40px auto 0;
      padding: 12px 30px;
      background: #2b6cb0;
      color: #fff;
      font-size: 1rem;
      text-align: center;
      text-decoration: none;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      transition: background 0.3s ease, transform 0.2s ease;
    }
    .back-button:hover {
      background: #2c5282;
      transform: translateY(-3px);
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1><%= @college.college %></h1>
      <div class="university-info">
        <p><strong>住所:</strong> <%= @college.address %>, <%= @college.city %>, <%= @college.state %>, <%= @college.zip %></p>
        <p><strong>公式ホームページ:</strong> <a href="<%= @college.website %>" target="_blank" style="color: #3498db; text-decoration: none;"><%= @college.website %></a></p>
      </div>
      <div class="social-links">
        <a href="https://www.instagram.com/" target="_blank" style="color: #E4405F;"><i class="fab fa-instagram"></i></a>
        <a href="https://www.facebook.com/" target="_blank" style="color: #1877F2;"><i class="fab fa-facebook"></i></a>
        <a href="https://twitter.com/" target="_blank" style="color: #1DA1F2;"><i class="fab fa-twitter"></i></a>
        <a href="https://www.youtube.com/" target="_blank" style="color: #FF0000;"><i class="fab fa-youtube"></i></a>
      </div>
    </header>

    <div class="media-section">
      <div class="media-item campus-photo">
        <img src="/images/sample-campus.jpg" alt="キャンパスの写真">
      </div>
      <div class="media-item ranking-box">
        <h2>Ranking by U.S. News & World Report</h2>
        <ul>
          <li>Regional Colleges Midwest 2位</li>
          <li>Best Value Schools 1位</li>
          <li>Civil 11位</li>
          <li>Best Undergraduate Engineering Programs 12位</li>
          <li>Top Performers on Social Mobility 33位</li>
        </ul>
      </div>
    </div>

    <div class="details-section">
    <div class="detail-card"><h2>州</h2><p><%= @college.state ? @college.state.gsub(/\s*\(.*?\)/, '') : "N/A" %></p></div>
        <div class="detail-card"><h2>私立/州立</h2><p><%= @college.privateorpublic %></p></div>
      <div class="detail-card"><h2>学生数</h2><p><%= @college.students %></p></div>
      <div class="detail-card"><h2>合格率</h2><p><%= @college.acceptance_rate ? "#{(@college.acceptance_rate * 100).round}%" : "N/A" %></p></div>
      <div class="detail-card"><h2>授業料</h2><p><%= @college.tuition ? "$#{number_with_delimiter(@college.tuition)}" : "N/A" %></p></div>
      <div class="detail-card"><h2>平均GPA</h2><p><%= @college.GPA || "N/A" %></p></div>
      <div class="detail-card"><h2>NCAA Division</h2><p><%= @college.Division || "N/A" %></p></div>
      <div class="detail-card"><h2>卒業率</h2><p><%= @college.graduation_rate ? "#{(@college.graduation_rate * 100).round}%" : "N/A" %></p></div>
    </div>

    <div class="ranking-activities-box">
      <div class="ranking-box"></div>
      <div class="activities-box">
        <h2>部活</h2>
        <p>ここに部活を追加</p>
      </div>
    </div>

    <div class="major-box">
      <h2>専攻</h2>
      <p><%= @college.major %></p>
    </div>

    <%= link_to '検索結果に戻る', results_path, class: "back-button" %>
  </div>
</body>
</html>


