<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "College Spark - アメリカ大学検索サイト" %></title>
    <meta name="description" content="<%= content_for(:description) || "6,300校以上のアメリカの大学から、あなたの条件に合った大学を検索できます。学費、合格率、専攻など詳細な情報で大学選びをサポートします。" %>">
    <meta name="keywords" content="<%= content_for(:keywords) || "アメリカ大学, 大学検索, 留学, 海外大学, College Spark, 大学選び, 学費, 合格率" %>">
    <meta name="author" content="College Spark">
    <meta name="robots" content="index, follow">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-TLR579VF');</script>
    <!-- End Google Tag Manager -->

    <!-- OGP Tags -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content_for(:og_title) || content_for(:title) || "College Spark - アメリカ大学検索サイト" %>">
    <meta property="og:description" content="<%= content_for(:og_description) || content_for(:description) || "6,300校以上のアメリカの大学から、あなたの条件に合った大学を検索できます。学費、合格率、専攻など詳細な情報で大学選びをサポートします。" %>">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:image" content="<%= content_for(:og_image) || asset_url('hero_background.jpg') %>">
    <meta property="og:site_name" content="College Spark">
    <meta property="og:locale" content="ja_JP">
    
    <!-- Twitter Cards -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@college__spark">
    <meta name="twitter:creator" content="@college__spark">
    <meta name="twitter:title" content="<%= content_for(:twitter_title) || content_for(:og_title) || content_for(:title) || "College Spark - アメリカ大学検索サイト" %>">
    <meta name="twitter:description" content="<%= content_for(:twitter_description) || content_for(:og_description) || content_for(:description) || "6,300校以上のアメリカの大学から、あなたの条件に合った大学を検索できます。" %>">
    <meta name="twitter:image" content="<%= content_for(:twitter_image) || content_for(:og_image) || asset_url('hero_background.jpg') %>">
    
    <!-- Canonical URL -->
    <link rel="canonical" href="<%= content_for(:canonical_url) || request.original_url %>">

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "College Spark",
      "url": "<%= request.base_url %>",
      "description": "6,300校以上のアメリカの大学から、あなたの条件に合った大学を検索・比較できるサイト",
      "potentialAction": {
        "@type": "SearchAction",
        "target": {
          "@type": "EntryPoint",
          "urlTemplate": "<%= request.base_url %>/results?college_name={search_term_string}"
        },
        "query-input": "required name=search_term_string"
      },
      "publisher": {
        "@type": "Organization",
        "name": "College Spark",
        "url": "<%= request.base_url %>"
      }
    }
    </script>

    <%= yield :head %>
    <%= yield :structured_data %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
    <link rel="apple-touch-icon" href="/favicon.ico">
    
    <!-- Preconnect to external domains -->
    <link rel="preconnect" href="https://cdn.jsdelivr.net">
    <link rel="preconnect" href="https://cdnjs.cloudflare.com">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preconnect" href="https://www.googletagmanager.com">
    <link rel="dns-prefetch" href="https://www.google-analytics.com">

    <%# Bootstrap CSS %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" media="print" onload="this.media='all'">
    <noscript><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"></noscript>
    
    <%# Font Awesome CSS %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", rel: "stylesheet" %>
    
    <%# Bootstrap JavaScript %>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
    
    <%= javascript_importmap_tags %>
    
    <style>
      /* Global reset to match /about page */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        overflow-x: hidden;
      }
      
      body {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      
      main {
        flex: 1;
        margin-top: 0;
        padding-top: 0;
      }
    </style>
  </head>

  <body>
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-TLR579VF"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
    
    <%= render 'shared/header' %>
    <main>
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
    
    <!-- フローティングCTAボタン -->
    <% unless controller_name == 'consultations' && action_name == 'new' %>
      <button id="floatingCTA" class="floating-cta-btn" onclick="openFloatingConsultation()">
        <i class="fas fa-comments"></i>
        <span>無料相談</span>
      </button>
    <% end %>
    
    <!-- 相談フォームポップアップ -->
    <div id="floatingConsultationPopup" class="floating-popup-overlay" style="display: none;">
      <div class="floating-popup">
        <button class="floating-popup-close" onclick="closeFloatingConsultation()">
          <i class="fas fa-times"></i>
        </button>
        
        <div class="floating-popup-content">
          <div class="floating-popup-header">
            <i class="fas fa-graduation-cap"></i>
            <h3>留学相談予約</h3>
            <p>お気軽にご相談ください</p>
          </div>
          
          <div class="floating-popup-features">
            <div class="floating-feature">
              <i class="fas fa-check"></i>
              <span>完全無料</span>
            </div>
            <div class="floating-feature">
              <i class="fas fa-check"></i>
              <span>オンライン対応</span>
            </div>
            <div class="floating-feature">
              <i class="fas fa-check"></i>
              <span>経験豊富なアドバイザー</span>
            </div>
          </div>
          
          <a href="/consultations/new" class="floating-cta-link">
            相談予約フォームへ
            <i class="fas fa-arrow-right"></i>
          </a>
        </div>
      </div>
    </div>
    
    <!-- Star Rating Widget -->

    <!-- Rating Widget -->
    <div id="ratingWidget" class="rating-widget" style="display: none;">
      <button class="widget-close" onclick="closeSurveyWidget()">&times;</button>
      <div class="rating-content">
        <p class="rating-text">ご意見をお聞かせください</p>
        <div class="star-rating">
          <span class="star" onclick="setRating(1)"><i class="fas fa-star"></i></span>
          <span class="star" onclick="setRating(2)"><i class="fas fa-star"></i></span>
          <span class="star" onclick="setRating(3)"><i class="fas fa-star"></i></span>
          <span class="star" onclick="setRating(4)"><i class="fas fa-star"></i></span>
          <span class="star" onclick="setRating(5)"><i class="fas fa-star"></i></span>
        </div>
      </div>
    </div>
    
    <!-- Detailed Survey Popup -->
    <div id="detailedSurvey" class="survey-overlay" style="display: none;">
      <div class="survey-popup">
        <button class="survey-close" onclick="closeDetailedSurvey()">&times;</button>
        <div class="survey-header">
          <h3>🎯 詳細アンケート</h3>
          <p>さらに詳しくお聞かせください（任意）</p>
        </div>
        
        <form id="detailedSurveyForm" class="survey-form">
          <div class="survey-question">
            <label>どのような目的でサイトを訪問されましたか？</label>
            <div class="survey-options">
              <label class="survey-option">
                <input type="radio" name="purpose" value="research">
                <span>留学の情報収集</span>
              </label>
              <label class="survey-option">
                <input type="radio" name="purpose" value="specific">
                <span>特定の大学を探している</span>
              </label>
              <label class="survey-option">
                <input type="radio" name="purpose" value="browsing">
                <span>なんとなく見ている</span>
              </label>
              <label class="survey-option">
                <input type="radio" name="purpose" value="other">
                <span>その他</span>
              </label>
            </div>
          </div>
          
          <div class="survey-question">
            <label>改善してほしい点があれば教えてください（任意）</label>
            <textarea name="feedback" rows="3" placeholder="ご意見・ご要望をお聞かせください"></textarea>
          </div>
          
          <div class="survey-buttons">
            <button type="button" class="survey-skip" onclick="closeDetailedSurvey()">スキップ</button>
            <button type="submit" class="survey-submit">送信する</button>
          </div>
        </form>
        
        <div class="survey-thanks" style="display: none;">
          <div class="thanks-icon">🎉✨</div>
          <h3>心より感謝申し上げます！</h3>
          <p>あなたの貴重なご意見は、より良いサービス作りの原動力となります。<br>
          <strong>College Spark</strong>をより使いやすく、より価値のあるものにするために<br>
          大切に活用させていただきます。</p>
          <div class="thanks-message">
            <p>🚀 今後ともよろしくお願いいたします</p>
          </div>
        </div>
      </div>
    </div>
    
    <style>
      /* フローティングCTAボタン */
      .floating-cta-btn {
        position: fixed;
        bottom: 30px;
        right: 30px;
        background: linear-gradient(135deg, #F5B531 0%, #F59E0B 100%);
        color: white;
        border: none;
        border-radius: 50px;
        padding: 14px 20px;
        font-size: 15px;
        font-weight: 700;
        cursor: pointer;
        box-shadow: 0 8px 24px rgba(245, 181, 49, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.2);
        transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        z-index: 1002;
        display: flex;
        align-items: center;
        gap: 8px;
        animation: softPulse 3s ease-in-out infinite;
        backdrop-filter: blur(8px);
        border: 1px solid rgba(255, 255, 255, 0.1);
      }
      
      @keyframes softPulse {
        0% { 
          transform: scale(1); 
          box-shadow: 0 8px 24px rgba(245, 181, 49, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }
        50% { 
          transform: scale(1.02); 
          box-shadow: 0 12px 32px rgba(245, 181, 49, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.3);
        }
        100% { 
          transform: scale(1); 
          box-shadow: 0 8px 24px rgba(245, 181, 49, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }
      }
      
      .floating-cta-btn:hover {
        transform: translateY(-2px) scale(1.05);
        background: linear-gradient(135deg, #F59E0B 0%, #D97706 100%);
        box-shadow: 0 16px 48px rgba(245, 181, 49, 0.6), inset 0 1px 0 rgba(255, 255, 255, 0.3);
        animation: none;
      }
      
      .floating-cta-btn i {
        font-size: 20px;
      }
      
      /* フローティングポップアップ */
      .floating-popup-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.6);
        z-index: 10000;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: fadeIn 0.3s ease;
      }
      
      .floating-popup {
        background: white;
        border-radius: 20px;
        max-width: 400px;
        width: 90%;
        padding: 30px;
        position: relative;
        animation: slideUp 0.4s ease;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      }
      
      @keyframes slideUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      
      .floating-popup-close {
        position: absolute;
        top: 15px;
        right: 15px;
        background: #f3f4f6;
        border: none;
        border-radius: 50%;
        width: 32px;
        height: 32px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
      }
      
      .floating-popup-close:hover {
        background: #e5e7eb;
        transform: rotate(90deg);
      }
      
      .floating-popup-header {
        text-align: center;
        margin-bottom: 25px;
      }
      
      .floating-popup-header i {
        font-size: 3rem;
        color: #ff7a00;
        margin-bottom: 15px;
        display: block;
      }
      
      .floating-popup-header h3 {
        font-size: 1.5rem;
        color: #2d3748;
        margin-bottom: 8px;
      }
      
      .floating-popup-header p {
        color: #6b7280;
        font-size: 1rem;
      }
      
      .floating-popup-features {
        display: flex;
        flex-direction: column;
        gap: 12px;
        margin-bottom: 25px;
      }
      
      .floating-feature {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 12px;
        background: #f8f9fa;
        border-radius: 10px;
        font-size: 0.95rem;
        color: #4a5568;
      }
      
      .floating-feature i {
        color: #10b981;
        font-size: 1rem;
      }
      
      .floating-cta-link {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        width: 100%;
        background: linear-gradient(135deg, #ff9224 0%, #ff7a00 100%);
        color: white;
        padding: 14px 24px;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 20px rgba(255, 122, 0, 0.3);
      }
      
      .floating-cta-link:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 25px rgba(255, 122, 0, 0.4);
        color: white;
        text-decoration: none;
      }
      
      .floating-cta-link i {
        transition: transform 0.3s ease;
      }
      
      .floating-cta-link:hover i {
        transform: translateX(3px);
      }
      
      /* モバイル対応 */
      @media (max-width: 768px) {
        .floating-cta-btn {
          bottom: 20px;
          right: 20px;
          padding: 12px 20px;
          font-size: 14px;
        }
        
        .floating-cta-btn span {
          display: none;
        }
        
        .floating-cta-btn i {
          font-size: 24px;
        }
        
        .floating-popup {
          padding: 25px 20px;
        }
      }
      
      @media (max-width: 480px) {
        .floating-cta-btn {
          bottom: 15px;
          right: 15px;
          width: 56px;
          height: 56px;
          padding: 0;
          border-radius: 50%;
          justify-content: center;
        }
      }
      
      .rating-widget {
        position: fixed;
        bottom: 20px;
        left: 20px;
        z-index: 1000;
        background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
        border-radius: 12px;
        padding: 24px 28px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
        max-width: 300px;
        color: white;
        border: 1px solid rgba(240, 177, 61, 0.2);
        display: none;
      }

      /* モバイル対応 */
      @media (max-width: 768px) {
        .rating-widget {
          bottom: 10px;
          left: 10px;
          right: 10px;
          max-width: none;
          padding: 20px;
          border-radius: 10px;
        }
      }

      @media (max-width: 480px) {
        .rating-widget {
          bottom: 8px;
          left: 8px;
          right: 8px;
          padding: 16px;
          border-radius: 8px;
        }
      }

      .rating-widget.show {
        display: block !important;
      }

      .rating-widget.hidden {
        display: none !important;
      }

      .widget-close {
        position: absolute;
        top: 10px;
        right: 10px;
        background: rgba(255, 255, 255, 0.1);
        border: none;
        color: rgba(255, 255, 255, 0.6);
        font-size: 18px;
        cursor: pointer;
        width: 28px;
        height: 28px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        padding: 0;
      }

      .widget-close:hover {
        background: rgba(255, 255, 255, 0.2);
        color: white;
      }

      .rating-content {
        text-align: center;
        position: relative;
      }

      .rating-subtext {
        margin-top: 12px;
        font-size: 13px;
        color: rgba(255, 255, 255, 0.6);
        font-weight: 400;
      }

      @media (max-width: 480px) {
        .rating-subtext {
          font-size: 12px;
          margin-top: 10px;
        }
      }
      
      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 0.9; }
      }
      
      @keyframes fadeOut {
        from { 
          opacity: 1;
          transform: scale(1);
        }
        to { 
          opacity: 0;
          transform: scale(0.8);
        }
      }
      
      /* トースト通知 */
      .toast-notification {
        position: fixed;
        bottom: 100px;
        left: 50%;
        transform: translateX(-50%) translateY(100px);
        background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%);
        color: white;
        padding: 16px 32px;
        border-radius: 50px;
        font-weight: 600;
        font-size: 16px;
        box-shadow: 0 8px 24px rgba(76, 175, 80, 0.3);
        z-index: 10001;
        opacity: 0;
        transition: all 0.3s ease-out;
      }
      
      .toast-notification.show {
        transform: translateX(-50%) translateY(0);
        opacity: 1;
      }
      
      
      .rating-text {
        margin: 0 0 16px 0;
        font-size: 15px;
        font-weight: 600;
        color: white;
        letter-spacing: 0.3px;
      }

      @media (max-width: 480px) {
        .rating-text {
          font-size: 14px;
          margin: 0 0 12px 0;
        }
      }

      .star-rating {
        display: flex;
        gap: 6px;
        justify-content: center;
      }

      .star {
        font-size: 24px;
        cursor: pointer;
        color: rgba(240, 177, 61, 0.5);
        transition: all 0.2s ease;
      }

      @media (max-width: 480px) {
        .star {
          font-size: 22px;
        }
      }

      .star:hover {
        color: rgb(240, 177, 61);
        transform: scale(1.15);
      }

      .star:active {
        transform: scale(1.25);
      }

      .star.active {
        color: rgb(240, 177, 61);
      }
      
      /* Detailed Survey Popup Styles */
      .survey-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.5);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 10000;
        animation: fadeIn 0.3s ease;
      }
      
      .survey-popup {
        background: white;
        border-radius: 16px;
        padding: 25px;
        max-width: 450px;
        width: 90%;
        max-height: 80vh;
        overflow-y: auto;
        position: relative;
        animation: slideIn 0.3s ease;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      }
      
      @media (max-width: 480px) {
        .survey-popup {
          width: 95%;
          padding: 20px;
          border-radius: 12px;
          max-height: 85vh;
        }
      }
      
      @keyframes slideIn {
        from { transform: translateY(-20px); opacity: 0; }
        to { transform: translateY(0); opacity: 1; }
      }
      
      .survey-close {
        position: absolute;
        top: 10px;
        right: 10px;
        width: 40px;
        height: 40px;
        border: none;
        background: #f3f4f6;
        border-radius: 50%;
        font-size: 24px;
        cursor: pointer;
        color: #6b7280;
        transition: all 0.2s ease;
        padding: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      
      .survey-close:hover {
        background: #e5e7eb;
        color: #374151;
      }
      
      .survey-header {
        text-align: center;
        margin-bottom: 25px;
      }
      
      .survey-header h3 {
        font-size: 1.3rem;
        color: #1f2937;
        margin-bottom: 8px;
        font-weight: 700;
      }
      
      .survey-header p {
        color: #6b7280;
        font-size: 0.9rem;
      }
      
      .survey-question {
        margin-bottom: 20px;
      }
      
      .survey-question label {
        display: block;
        font-weight: 600;
        color: #374151;
        margin-bottom: 10px;
        font-size: 0.9rem;
      }
      
      .survey-options {
        display: flex;
        flex-direction: column;
        gap: 8px;
      }
      
      .survey-option {
        display: flex;
        align-items: center;
        padding: 10px 12px;
        border: 2px solid #e5e7eb;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.2s ease;
        font-size: 0.85rem;
      }
      
      .survey-option:hover {
        border-color: #fbbf24;
        background: #fef3c7;
      }
      
      .survey-option input[type="radio"] {
        margin-right: 8px;
        cursor: pointer;
      }
      
      .survey-option input[type="radio"]:checked + span {
        font-weight: 600;
        color: #d97706;
      }
      
      .survey-question textarea {
        width: 100%;
        padding: 10px;
        border: 2px solid #e5e7eb;
        border-radius: 8px;
        resize: vertical;
        font-family: inherit;
        font-size: 0.85rem;
      }
      
      .survey-question textarea:focus {
        outline: none;
        border-color: #fbbf24;
      }
      
      .survey-buttons {
        display: flex;
        gap: 10px;
        margin-top: 20px;
      }
      
      .survey-skip {
        flex: 1;
        padding: 12px;
        background: #f3f4f6;
        color: #6b7280;
        border: none;
        border-radius: 8px;
        font-size: 0.9rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s ease;
      }
      
      .survey-skip:hover {
        background: #e5e7eb;
        color: #374151;
      }
      
      .survey-submit {
        flex: 2;
        padding: 12px;
        background: linear-gradient(135deg, #fbbf24, #f59e0b);
        color: white;
        border: none;
        border-radius: 8px;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
      }
      
      .survey-submit:hover {
        background: linear-gradient(135deg, #f59e0b, #d97706);
        transform: translateY(-1px);
        box-shadow: 0 6px 15px rgba(245, 158, 11, 0.3);
      }
      
      .survey-thanks {
        text-align: center;
        padding: 40px 20px;
        background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
        border-radius: 12px;
        animation: thanksFadeIn 0.6s ease-out;
      }
      
      @keyframes thanksFadeIn {
        from {
          opacity: 0;
          transform: scale(0.9);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }
      
      .thanks-icon {
        font-size: 4rem;
        margin-bottom: 20px;
        animation: thanksIconBounce 1s ease-out;
      }
      
      @keyframes thanksIconBounce {
        0%, 20%, 50%, 80%, 100% {
          transform: translateY(0);
        }
        40% {
          transform: translateY(-10px);
        }
        60% {
          transform: translateY(-5px);
        }
      }
      
      .survey-thanks h3 {
        font-size: 1.5rem;
        color: #0f172a;
        margin-bottom: 16px;
        font-weight: 700;
      }
      
      .survey-thanks p {
        color: #475569;
        font-size: 1rem;
        line-height: 1.6;
        margin-bottom: 20px;
      }
      
      .thanks-message {
        background: rgba(59, 130, 246, 0.1);
        padding: 16px;
        border-radius: 8px;
        border-left: 4px solid #3b82f6;
      }
      
      .thanks-message p {
        color: #1e40af;
        font-weight: 600;
        margin: 0;
        font-size: 1.1rem;
      }
      
      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }
      
      @media (max-width: 768px) {
        .survey-popup {
          padding: 20px;
          margin: 20px;
        }
      }

      @media (max-width: 480px) {
        .survey-popup {
          padding: 15px;
        }
      }
    </style>
    
    <script>
      // 星評価ウィジェット
      function setRating(rating) {
        // 星のアニメーション
        const stars = document.querySelectorAll('.star');
        stars.forEach((star, index) => {
          if (index < rating) {
            star.style.color = 'rgb(240, 177, 61)';
            star.style.transform = 'scale(1.2)';
            star.classList.add('active');
          }
        });
        
        // 評価メッセージ
        const messages = [
          '貴重なご意見ありがとうございます！',
          'フィードバックありがとうございます！',
          'ご評価ありがとうございます！',
          '素晴らしい評価をありがとうございます！',
          '最高評価をありがとうございます！🎉'
        ];
        
        // カスタムトースト通知
        showToast(messages[rating - 1]);
        
        // 評価をLocalStorageに保存（即座に保存）
        localStorage.setItem('collegeSparkRating', rating.toString());
        localStorage.setItem('collegeSparkRatingDate', new Date().toISOString());
        console.log('Rating saved to localStorage:', rating);
        
        // LocalStorageへの保存を確認
        const savedRating = localStorage.getItem('collegeSparkRating');
        console.log('Confirmed saved rating:', savedRating);
        
        // ウィジェットをフェードアウト
        const widget = document.getElementById('ratingWidget');
        widget.style.animation = 'fadeOut 0.5s ease-out forwards';
        
        setTimeout(() => {
          widget.classList.add('hidden');
          widget.style.display = 'none';
          console.log('Widget hidden after rating');
          // 詳細アンケートを表示
          document.getElementById('detailedSurvey').style.display = 'flex';
          document.body.style.overflow = 'hidden';
        }, 500);
      }
      
      // トースト通知を表示
      function showToast(message) {
        const toast = document.createElement('div');
        toast.className = 'toast-notification';
        toast.textContent = message;
        document.body.appendChild(toast);
        
        setTimeout(() => {
          toast.classList.add('show');
        }, 100);
        
        setTimeout(() => {
          toast.classList.remove('show');
          setTimeout(() => toast.remove(), 300);
        }, 3000);
      }
      
      // アンケートウィジェットを閉じる
      function closeSurveyWidget() {
        // 即座にLocalStorageに保存
        localStorage.setItem('collegeSparkSurveyClosed', 'true');
        console.log('Survey closed flag saved to localStorage');
        
        const widget = document.getElementById('ratingWidget');
        widget.style.animation = 'fadeOut 0.3s ease-out forwards';
        setTimeout(() => {
          widget.classList.add('hidden');
          widget.style.display = 'none';
          console.log('Widget closed by user');
        }, 300);
        
        // LocalStorageへの保存を確認
        const savedClosed = localStorage.getItem('collegeSparkSurveyClosed');
        console.log('Confirmed survey closed flag:', savedClosed);
      }
      
      // 詳細アンケートを閉じる
      function closeDetailedSurvey() {
        const currentStatus = localStorage.getItem('collegeSparkDetailedSurvey');
        
        document.getElementById('detailedSurvey').style.display = 'none';
        document.body.style.overflow = '';
        
        // まだ回答していない場合（スキップの場合）
        if (!currentStatus || currentStatus === 'skipped') {
          localStorage.setItem('collegeSparkDetailedSurvey', 'skipped');
          showToast('💭 お時間をいただき、ありがとうございました！');
        }
      }
      
      // アンケート関連のLocalStorageデータのみを削除（開発・テスト用）
      function clearSurveyData() {
        console.log('Clearing all survey data...');
        localStorage.removeItem('collegeSparkRating');
        localStorage.removeItem('collegeSparkRatingDate');
        localStorage.removeItem('collegeSparkSurveyClosed');
        localStorage.removeItem('collegeSparkDetailedSurvey');
        localStorage.removeItem('collegeSparkDetailedSurveyData');
        
        // ウィジェットを即座に表示
        const widget = document.getElementById('ratingWidget');
        widget.classList.remove('hidden');
        widget.style.display = 'block';
        
        console.log('Survey data cleared and widget restored.');
      }
      
      // 強制的にウィジェットを表示（デバッグ用）
      function forceShowWidget() {
        let widget = document.getElementById('ratingWidget');

        if (!widget) {
          console.log('Widget not found, creating new one...');
          // ウィジェットが存在しない場合は作成
          const widgetHTML = `
            <div id="ratingWidget" class="rating-widget" style="display: block;">
              <button class="widget-close" onclick="closeSurveyWidget()">&times;</button>
              <div class="rating-content">
                <p class="rating-text">ご意見をお聞かせください</p>
                <div class="star-rating">
                  <span class="star" onclick="setRating(1)"><i class="fas fa-star"></i></span>
                  <span class="star" onclick="setRating(2)"><i class="fas fa-star"></i></span>
                  <span class="star" onclick="setRating(3)"><i class="fas fa-star"></i></span>
                  <span class="star" onclick="setRating(4)"><i class="fas fa-star"></i></span>
                  <span class="star" onclick="setRating(5)"><i class="fas fa-star"></i></span>
                </div>
              </div>
            </div>
          `;
          document.body.insertAdjacentHTML('beforeend', widgetHTML);
          widget = document.getElementById('ratingWidget');
        } else {
          // 既存ウィジェットを表示
          widget.style.display = 'block';
          widget.classList.remove('hidden');
        }

        // ホバーエフェクトを設定
        setupStarHoverEffects();

        showToast('アンケートを強制表示しました');
        console.log('Widget forced to show');
      }
      
      // 強制的にウィジェットを非表示（デバッグ用）
      function forceHideWidget() {
        const widget = document.getElementById('ratingWidget');
        widget.classList.add('hidden');
        widget.classList.remove('show');
        console.log('Widget forced to hide');
      }
      
      // デバッグ用：なぜウィジェットが表示されないかを詳しく調べる
      function debugWidgetVisibility() {
        console.log('🔍 === WIDGET VISIBILITY DEBUG ===');
        
        const widget = document.getElementById('ratingWidget');
        console.log('Widget element exists:', !!widget);
        
        if (widget) {
          console.log('Widget display style:', widget.style.display);
          console.log('Widget computed display:', window.getComputedStyle(widget).display);
          console.log('Widget classes:', widget.className);
        }
        
        const status = checkSurveyStatus();
        const hasRated = status.rating !== null && status.rating !== '';
        const wasClosedByUser = status.surveyClosed === 'true';
        
        console.log('🎯 Visibility Decision:');
        console.log('- Has rated (should hide):', hasRated);
        console.log('- Was closed by user (should hide):', wasClosedByUser);
        console.log('- Should be visible:', !hasRated && !wasClosedByUser);
        
        return {
          widgetExists: !!widget,
          shouldBeVisible: !hasRated && !wasClosedByUser,
          actuallyVisible: widget ? window.getComputedStyle(widget).display !== 'none' : false
        };
      }
      
      // 初期化処理を独立した関数として定義
      function initializeSurveyWidget() {
        console.log('🔍 Initializing survey widget...');
        
        // LocalStorageの状態を確認
        const status = checkSurveyStatus();
        
        // 評価済みまたは明示的に閉じられた場合のみ非表示
        const hasRated = status.rating !== null && status.rating !== '';
        const wasClosedByUser = status.surveyClosed === 'true';
        
        // 詳細アンケートのスキップは星評価とは無関係なので除外
        const widget = document.getElementById('ratingWidget');
        
        console.log('=== Widget Display Logic ===');
        console.log('Widget element found:', !!widget);
        console.log('Rating value:', status.rating);
        console.log('Survey closed by user:', status.surveyClosed);
        console.log('Has rated:', hasRated);
        console.log('Was closed by user:', wasClosedByUser);
        console.log('Widget should be hidden:', hasRated || wasClosedByUser);
        
        if (!widget) {
          console.error('❌ Widget element not found!');
          return false;
        }
        
        // 評価済みまたは明示的に閉じられた場合のみ非表示
        if (hasRated || wasClosedByUser) {
          console.log('❌ Hiding widget permanently...');
          widget.style.display = 'none';
          return false; // ウィジェットは表示されない
        }
        
        console.log('✅ Showing widget (not rated and not closed by user)...');
        widget.style.display = 'block';
        widget.classList.remove('hidden');
        widget.classList.add('show');
        
        // 強制的にスタイルを再適用
        widget.style.background = 'linear-gradient(135deg, rgba(255, 146, 36, 0.7) 0%, rgba(255, 122, 0, 0.7) 100%)';
        
        console.log('✅ Widget display set to block');
        
        // 星のホバーエフェクトを設定
        setupStarHoverEffects();
        
        // 詳細アンケートフォームの処理も設定
        setupDetailedSurveyForm();
        
        return true; // ウィジェットが表示された
      }
      
      // 星のホバーエフェクトを設定する関数
      function setupStarHoverEffects() {
        const stars = document.querySelectorAll('.star');
        stars.forEach((star, index) => {
          star.addEventListener('mouseenter', function() {
            stars.forEach((s, i) => {
              if (i <= index) {
                s.style.color = 'rgb(240, 177, 61)';
              } else {
                s.style.color = '';
              }
            });
          });
        });

        // マウスが離れたら元に戻す
        const starRating = document.querySelector('.star-rating');
        if (starRating) {
          starRating.addEventListener('mouseleave', function() {
            stars.forEach(star => {
              if (!star.classList.contains('active')) {
                star.style.color = '';
              }
            });
          });
        }
      }
      
      // 詳細アンケートフォームの処理を設定する関数
      function setupDetailedSurveyForm() {
        const detailedForm = document.getElementById('detailedSurveyForm');
        
        // 既にイベントリスナーが設定されている場合はスキップ
        if (detailedForm.dataset.listenerAdded === 'true') {
          console.log('📋 Detailed survey form listener already added, skipping...');
          return;
        }
        
        console.log('📋 Setting up detailed survey form...');
        
        detailedForm.addEventListener('submit', function(e) {
          e.preventDefault();
          console.log('📋 Detailed survey form submitted');
          
          const formData = new FormData(detailedForm);
          const data = {
            purpose: formData.get('purpose'),
            feedback: formData.get('feedback'),
            rating: localStorage.getItem('collegeSparkRating'),
            timestamp: new Date().toISOString()
          };
          
          // サーバーに送信
          fetch('/surveys', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
              'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
            },
            body: JSON.stringify(data)
          })
          .then(response => response.json())
          .then(result => {
            if (result.status === 'success') {
              localStorage.setItem('collegeSparkDetailedSurvey', 'completed');
              console.log('📋 Survey data saved successfully');
            } else {
              console.error('📋 Survey save failed:', result.message);
              localStorage.setItem('collegeSparkDetailedSurvey', 'completed');
            }
          })
          .catch(error => {
            console.error('📋 Survey submission error:', error);
            localStorage.setItem('collegeSparkDetailedSurveyData', JSON.stringify(data));
            localStorage.setItem('collegeSparkDetailedSurvey', 'completed');
          });
          
          // お礼トースト通知を表示
          showToast('🎉 詳細アンケートにご回答いただき、ありがとうございました！');
          
          // Thank youメッセージ表示
          console.log('📋 Showing thank you message...');
          detailedForm.style.display = 'none';
          document.querySelector('.survey-header').style.display = 'none';
          document.querySelector('.survey-thanks').style.display = 'block';
          
          // アンケート完了をマーク
          localStorage.setItem('collegeSparkDetailedSurvey', 'completed');
          
          // 6秒後に自動的に閉じる
          setTimeout(() => {
            closeDetailedSurvey();
            showToast('✨ 貴重なご意見をありがとうございました！');
          }, 6000);
        });
        
        // リスナーが追加されたことをマーク
        detailedForm.dataset.listenerAdded = 'true';
        console.log('📋 Detailed survey form setup complete');
        
        // 背景クリックで詳細アンケートを閉じる処理も設定
        const detailedSurvey = document.getElementById('detailedSurvey');
        if (detailedSurvey.dataset.backgroundListenerAdded !== 'true') {
          detailedSurvey.addEventListener('click', function(e) {
            if (e.target === this) {
              closeDetailedSurvey();
            }
          });
          detailedSurvey.dataset.backgroundListenerAdded = 'true';
        }
      }
      
      // テスト用リセット機能（ブラウザボタン用）
      function resetSurveyForTesting() {
        console.log('🔄 Testing: Resetting survey data...');
        
        // 既存ウィジェットを削除
        const existingWidget = document.getElementById('ratingWidget');
        if (existingWidget) {
          existingWidget.remove();
          console.log('🗑️ Removed existing widget');
        }
        
        // 詳細アンケートも閉じる
        const detailedSurvey = document.getElementById('detailedSurvey');
        if (detailedSurvey) {
          detailedSurvey.style.display = 'none';
          document.body.style.overflow = '';
        }
        
        // LocalStorageを完全にクリア
        const keysToRemove = [
          'collegeSparkRating',
          'collegeSparkRatingDate', 
          'collegeSparkSurveyClosed',
          'collegeSparkDetailedSurvey',
          'collegeSparkDetailedSurveyData'
        ];
        
        keysToRemove.forEach(key => {
          localStorage.removeItem(key);
          console.log(`🧹 Removed: ${key}`);
        });
        
        // 初期化フラグをリセット
        window.surveyWidgetInitialized = false;
        
        // 新しいウィジェットを作成
        const widgetHTML = `
          <div id="ratingWidget" class="rating-widget" style="display: none;">
            <button class="widget-close" onclick="closeSurveyWidget()">&times;</button>
            <div class="rating-content">
              <p class="rating-text">ご意見をお聞かせください</p>
              <div class="star-rating">
                <span class="star" onclick="setRating(1)"><i class="fas fa-star"></i></span>
                <span class="star" onclick="setRating(2)"><i class="fas fa-star"></i></span>
                <span class="star" onclick="setRating(3)"><i class="fas fa-star"></i></span>
                <span class="star" onclick="setRating(4)"><i class="fas fa-star"></i></span>
                <span class="star" onclick="setRating(5)"><i class="fas fa-star"></i></span>
              </div>
            </div>
          </div>
        `;
        
        // HTMLを挿入
        document.body.insertAdjacentHTML('beforeend', widgetHTML);
        console.log('✨ Created new widget');
        
        // 少し待ってから初期化処理を実行
        setTimeout(() => {
          const widgetShown = initializeSurveyWidget();
          
          // トースト通知
          if (widgetShown) {
            showToast('🔄 アンケートがリセットされました！');
          } else {
            showToast('❌ リセットに失敗しました');
          }
          
          console.log('✅ Survey reset complete - widget restored:', widgetShown);
        }, 100);
      }
      
      // LocalStorageの全状態を確認
      function checkSurveyStatus() {
        const status = {
          rating: localStorage.getItem('collegeSparkRating'),
          ratingDate: localStorage.getItem('collegeSparkRatingDate'),
          surveyClosed: localStorage.getItem('collegeSparkSurveyClosed'),
          detailedSurvey: localStorage.getItem('collegeSparkDetailedSurvey'),
          detailedSurveyData: localStorage.getItem('collegeSparkDetailedSurveyData')
        };
        console.log('=== Survey LocalStorage Status ===');
        console.log('Rating:', status.rating);
        console.log('Rating Date:', status.ratingDate);
        console.log('Survey Closed:', status.surveyClosed);
        console.log('Detailed Survey:', status.detailedSurvey);
        console.log('Detailed Survey Data:', status.detailedSurveyData);
        
        // 値の詳細分析
        console.log('--- Value Analysis ---');
        console.log('rating !== null:', status.rating !== null);
        console.log('rating !== "":', status.rating !== '');
        console.log('surveyClosed === "true":', status.surveyClosed === 'true');
        
        return status;
      }
      
      // 初期化フラグ
      window.surveyWidgetInitialized = false;
      
      // ページ読み込み時の処理
      function initializeOnLoad() {
        console.log('📄 Starting initialization...');
        const result = initializeSurveyWidget();
        console.log('📄 Initialization result:', result);
        return result;
      }
      
      // 複数の方法で初期化を試行
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initializeOnLoad);
      } else {
        // DOM already loaded
        console.log('📄 DOM already loaded, initializing immediately...');
        initializeOnLoad();
      }
      
      // 追加の安全策：windowのloadイベントでも実行
      window.addEventListener('load', function() {
        console.log('📄 Window load event - checking if widget is visible...');
        const widget = document.getElementById('ratingWidget');
        if (widget && window.getComputedStyle(widget).display === 'none') {
          console.log('📄 Widget still hidden, re-initializing...');
          initializeOnLoad();
        }
      });
      
      // さらなる安全策：タイマーでチェック
      setTimeout(() => {
        const widget = document.getElementById('ratingWidget');
        if (widget && window.getComputedStyle(widget).display === 'none') {
          const status = checkSurveyStatus();
          const hasRated = status.rating !== null && status.rating !== '';
          const wasClosedByUser = status.surveyClosed === 'true';
          
          if (!hasRated && !wasClosedByUser) {
            console.log('⏰ Timer check: Widget should be visible but is hidden, forcing display...');
            widget.style.display = 'block';
          }
        }
      }, 1000);
      
      // 古いDOMContentLoadedリスナーは削除済み
      
      // フローティングCTAの制御
      function openFloatingConsultation() {
        const popup = document.getElementById('floatingConsultationPopup');
        popup.style.display = 'flex';
        document.body.style.overflow = 'hidden';
      }
      
      function closeFloatingConsultation() {
        const popup = document.getElementById('floatingConsultationPopup');
        popup.style.display = 'none';
        document.body.style.overflow = '';
      }
      
      // オーバーレイクリックで閉じる
      document.addEventListener('DOMContentLoaded', function() {
        const overlay = document.getElementById('floatingConsultationPopup');
        if (overlay) {
          overlay.addEventListener('click', function(e) {
            if (e.target === overlay) {
              closeFloatingConsultation();
            }
          });
        }
      });
    </script>
  </body>
</html>
