<div class="columns-container">
  <div class="columns-header">
    <div class="header-decoration">
      <div class="decoration-line left"></div>
      <div class="header-icon-container">
        <div class="header-icon">✒️</div>
        <div class="icon-ripple"></div>
      </div>
      <div class="decoration-line right"></div>
    </div>
    
    <h1 class="columns-title">
      <span class="title-main">
        <span class="title-letter">コ</span>
        <span class="title-letter">ラ</span>
        <span class="title-letter">ム</span>
      </span>
      <span class="title-subtitle">Column</span>
    </h1>
    
    <p class="columns-subtitle">
      深い洞察と体験談を通じて、<br class="mobile-break">
      <span class="highlight-text">留学の本質</span>を探る
    </p>
    
    <div class="header-stats">
      <div class="stat-item">
        <div class="stat-icon">📚</div>
        <div class="stat-content">
          <span class="stat-number"><%= @columns.length %></span>
          <span class="stat-label">記事</span>
        </div>
      </div>
      <div class="stat-divider">•</div>
      <div class="stat-item">
        <div class="stat-icon">⏱️</div>
        <div class="stat-content">
          <span class="stat-number">3</span>
          <span class="stat-label">分で読める</span>
        </div>
      </div>
      <div class="stat-divider">•</div>
      <div class="stat-item">
        <div class="stat-icon">✨</div>
        <div class="stat-content">
          <span class="stat-number">深い洞察</span>
          <span class="stat-label">をお届け</span>
        </div>
      </div>
    </div>
    
    <div class="scroll-indicator">
      <div class="scroll-text">記事を読む</div>
      <div class="scroll-arrow">↓</div>
    </div>
  </div>

  <div class="columns-grid">
    <% @columns.each do |column| %>
      <article class="column-card">
        <div class="column-header">
          <div class="column-meta">
            <span class="column-badge">コラム</span>
            <div class="column-tags">
              <% column[:tags]&.each do |tag| %>
                <span class="tag"><%= tag %></span>
              <% end %>
            </div>
          </div>
          <time class="column-date"><%= column[:published_at] %></time>
        </div>

        <div class="column-content">
          <h2 class="column-title">
            <%= link_to column[:title], column[:url], class: "column-link" %>
          </h2>
          
          <p class="column-subtitle"><%= column[:subtitle] %></p>
          
          <div class="column-excerpt">
            <%= column[:excerpt] %>
          </div>
          
          <div class="column-footer">
            <div class="author-section">
              <div class="author-avatar">
                <span><%= column[:author][0] %></span>
              </div>
              <div class="author-info">
                <span class="author-name"><%= column[:author] %></span>
                <span class="author-bio"><%= column[:author_bio] %></span>
              </div>
            </div>
            
            <%= link_to column[:url], class: "read-column-btn" do %>
              この記事を読む
              <i class="fas fa-arrow-right"></i>
            <% end %>
          </div>
        </div>
      </article>
    <% end %>
  </div>
</div>

<style>
  /* コラム専用スタイル */
  .columns-container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 40px 20px;
    font-family: 'Georgia', 'Times New Roman', serif;
  }

  .columns-header {
    text-align: center;
    margin-bottom: 80px;
    padding: 60px 0 50px;
    background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
    border-radius: 20px;
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
  }

  .columns-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="dots" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="0.8" fill="%23e2e8f0" opacity="0.3"/></pattern></defs><rect width="100" height="100" fill="url(%23dots)"/></svg>') repeat;
    z-index: 1;
  }

  .columns-header > * {
    position: relative;
    z-index: 2;
  }

  .header-decoration {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 30px;
    gap: 20px;
  }

  .decoration-line {
    width: 60px;
    height: 2px;
    background: linear-gradient(90deg, transparent, #cbd5e0, transparent);
    position: relative;
  }

  .decoration-line.left {
    background: linear-gradient(90deg, transparent, #cbd5e0);
  }

  .decoration-line.right {
    background: linear-gradient(90deg, #cbd5e0, transparent);
  }

  .header-icon-container {
    position: relative;
    display: inline-block;
  }

  .header-icon {
    font-size: 28px;
    padding: 12px;
    background: white;
    border-radius: 50%;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    border: 2px solid #f1f5f9;
    position: relative;
    z-index: 2;
    animation: iconFloat 3s ease-in-out infinite;
  }

  .icon-ripple {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: rgba(102, 126, 234, 0.2);
    animation: ripple 2s ease-out infinite;
  }

  @keyframes iconFloat {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-5px); }
  }

  @keyframes ripple {
    0% {
      transform: translate(-50%, -50%) scale(0.8);
      opacity: 1;
    }
    100% {
      transform: translate(-50%, -50%) scale(1.5);
      opacity: 0;
    }
  }

  .columns-title {
    margin-bottom: 25px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }

  .title-main {
    font-size: 52px;
    font-weight: 300;
    color: #1a202c;
    letter-spacing: 3px;
    line-height: 1;
    display: flex;
    gap: 8px;
  }

  .title-letter {
    display: inline-block;
    animation: letterDrop 0.6s ease-out forwards;
    opacity: 0;
    transform: translateY(-20px);
  }

  .title-letter:nth-child(1) { animation-delay: 0.1s; }
  .title-letter:nth-child(2) { animation-delay: 0.2s; }
  .title-letter:nth-child(3) { animation-delay: 0.3s; }

  @keyframes letterDrop {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .title-subtitle {
    font-size: 16px;
    font-weight: 500;
    color: #718096;
    letter-spacing: 4px;
    text-transform: uppercase;
    opacity: 0.8;
  }

  .columns-subtitle {
    font-size: 20px;
    color: #4a5568;
    font-weight: 300;
    margin: 0 0 40px 0;
    line-height: 1.6;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .highlight-text {
    color: #667eea;
    font-weight: 500;
    position: relative;
  }

  .highlight-text::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, #667eea, #764ba2);
    border-radius: 1px;
    opacity: 0.6;
  }

  .mobile-break {
    display: none;
  }

  .header-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
    padding: 20px;
    background: rgba(255, 255, 255, 0.8);
    border-radius: 15px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.6);
    max-width: 500px;
    margin: 0 auto;
  }

  .stat-item {
    display: flex;
    align-items: center;
    gap: 8px;
    transition: transform 0.3s ease;
  }

  .stat-item:hover {
    transform: translateY(-2px);
  }

  .stat-icon {
    font-size: 18px;
    width: 30px;
    height: 30px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(102, 126, 234, 0.1);
    border-radius: 50%;
    flex-shrink: 0;
  }

  .stat-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
  }

  .stat-number {
    font-size: 18px;
    font-weight: 600;
    color: #1a202c;
    line-height: 1;
  }

  .stat-label {
    font-size: 12px;
    color: #718096;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-weight: 500;
  }

  .stat-divider {
    color: #cbd5e0;
    font-size: 16px;
    margin: 0 5px;
  }

  .scroll-indicator {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    margin-top: 40px;
    opacity: 0.7;
    transition: opacity 0.3s ease;
  }

  .scroll-indicator:hover {
    opacity: 1;
  }

  .scroll-text {
    font-size: 14px;
    color: #718096;
    font-weight: 500;
    letter-spacing: 0.5px;
  }

  .scroll-arrow {
    font-size: 20px;
    color: #667eea;
    animation: bounce 2s ease-in-out infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-8px); }
    60% { transform: translateY(-4px); }
  }

  .columns-grid {
    display: grid;
    gap: 50px;
  }

  .column-card {
    background: white;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    overflow: hidden;
    transition: all 0.3s ease;
    border: 1px solid #f1f1f1;
  }

  .column-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
  }

  .column-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 30px 30px 0;
    margin-bottom: 25px;
  }

  .column-meta {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .column-badge {
    display: inline-block;
    padding: 6px 16px;
    background: #1a202c;
    color: white;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    border-radius: 20px;
    width: fit-content;
  }

  .column-tags {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .tag {
    display: inline-block;
    padding: 4px 10px;
    background: #f7fafc;
    color: #4a5568;
    font-size: 12px;
    font-weight: 500;
    border-radius: 12px;
    border: 1px solid #e2e8f0;
  }

  .column-date {
    color: #a0aec0;
    font-size: 14px;
    font-style: italic;
    white-space: nowrap;
  }

  .column-content {
    padding: 0 30px 30px;
  }

  .column-title {
    font-size: 32px;
    font-weight: 400;
    line-height: 1.3;
    margin-bottom: 15px;
    color: #1a202c;
  }

  .column-link {
    text-decoration: none;
    color: inherit;
    transition: color 0.3s ease;
  }

  .column-link:hover {
    color: #667eea;
    text-decoration: none;
  }

  .column-subtitle {
    font-size: 20px;
    color: #718096;
    font-style: italic;
    margin-bottom: 25px;
    font-weight: 300;
    line-height: 1.4;
  }

  .column-excerpt {
    font-size: 16px;
    line-height: 1.7;
    color: #4a5568;
    margin-bottom: 30px;
  }

  .column-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px solid #f1f1f1;
    padding-top: 25px;
  }

  .author-section {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .author-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: linear-gradient(135deg, #667eea, #764ba2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 600;
    font-size: 18px;
  }

  .author-info {
    display: flex;
    flex-direction: column;
  }

  .author-name {
    font-weight: 600;
    color: #1a202c;
    font-size: 15px;
    margin-bottom: 2px;
  }

  .author-bio {
    font-size: 13px;
    color: #718096;
  }

  .read-column-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: #1a202c;
    color: white;
    padding: 12px 24px;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 500;
    font-size: 14px;
    transition: all 0.3s ease;
    font-family: -apple-system, BlinkMacSystemFont, sans-serif;
  }

  .read-column-btn:hover {
    background: #2d3748;
    transform: translateY(-1px);
    color: white;
    text-decoration: none;
  }

  .read-column-btn i {
    font-size: 12px;
    transition: transform 0.3s ease;
  }

  .read-column-btn:hover i {
    transform: translateX(3px);
  }

  /* レスポンシブ */
  @media (max-width: 768px) {
    .columns-container {
      padding: 20px 15px;
    }

    .columns-header {
      margin-bottom: 60px;
      padding: 40px 20px 35px;
    }

    .header-decoration {
      margin-bottom: 25px;
      gap: 15px;
    }

    .decoration-line {
      width: 40px;
    }

    .header-icon {
      font-size: 24px;
      padding: 10px;
    }

    .title-main {
      font-size: 36px;
      letter-spacing: 2px;
    }

    .title-subtitle {
      font-size: 14px;
      letter-spacing: 3px;
    }

    .columns-subtitle {
      font-size: 18px;
      margin-bottom: 30px;
      padding: 0 10px;
    }

    .mobile-break {
      display: block;
    }

    .header-stats {
      gap: 15px;
      padding: 15px;
      flex-direction: column;
    }

    .stat-item {
      flex-direction: row;
      gap: 8px;
    }

    .stat-divider {
      display: none;
    }

    .column-header {
      flex-direction: column;
      gap: 15px;
      align-items: flex-start;
    }

    .column-content {
      padding: 0 20px 20px;
    }

    .column-title {
      font-size: 24px;
    }

    .column-subtitle {
      font-size: 18px;
    }

    .column-footer {
      flex-direction: column;
      gap: 20px;
      align-items: flex-start;
    }

    .read-column-btn {
      align-self: stretch;
      justify-content: center;
    }
  }
</style>